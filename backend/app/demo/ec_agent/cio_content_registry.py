"""Import side-effect registry of per-scenario CIO content (see ``cio_content``).

The ten Experience Center scenarios are specs on ``spec_engine``, which carries its own plan text,
findings and final state; none registers extra CIO content.
"""

from __future__ import annotations
