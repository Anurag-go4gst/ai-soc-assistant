# Assertion-change ledger — EC CIO coherence plan

Every pre-existing test assertion modified by `plans/2026-09-24_1635_ec-cio-coherence-and-lifecycle.md`,
with old → new and why. A changed assertion without a row here fails review.

| Item | Test | Old | New | Why |
|------|------|-----|-----|-----|
| A2 | `backend/app/tests/test_s1_agent_workflow.py::test_s1_agent_plan_ready_on_initial_turn` | opening narrative contains "splunk and mcp tools and rag guidelines" | contains "nothing runs until you approve" and **not** the boilerplate | The boilerplate phrase was the defect (content review X3). Other intent pins (IP, "last 30 days", no "suspicious") unchanged. |
| A2 | `backend/app/tests/test_s2_agent_workflow.py::test_s2_agent_plan_ready_on_initial_turn` | narrative contains boilerplate + "collecting and analyzing logs" | contains "nothing runs until you approve", keeps "customer-facing ai assistant", boilerplate absent | Same as above; "collecting and analyzing logs" was generic filler. |
| A2 | `backend/app/tests/test_s2_agent_workflow.py::test_s2_investigation_tools_are_only_onboarded_connectors` | rendered plan tool labels == {"Splunk MCP", "SOC-KB"} | rendered `tool_ids` == {"splunk_mcp", "soc_kb"} | Labels now come from the tool catalog ("SOC-KB (RAG)"). The intent — only onboarded connectors — is asserted on stable ids. The step-def assertion is unchanged. |
| A2 | `backend/app/tests/test_s7_agent_workflow.py::test_s7_agent_plan_ready_on_initial_turn` | narrative contains boilerplate | contains "nothing runs until you approve", boilerplate absent | Same as S1. "retired", "ot" and the negative pins unchanged. |
| A2 | `frontend/src/components/ec/s1Workspace.test.tsx` (agentLifecycleScrollTarget) | INVESTIGATION_COMPLETE / COMPLETE → `[data-ec-section="executive-summary"]` | → `[data-ec-section="executive-brief"], [data-ec-section="executive-summary"]` | S2/S4/S7 have an empty `executive_summary`, so the old target never existed and the page did not scroll to the outcome. The comma selector prefers the new brief and falls back to the summary. |
| A3.1 | `backend/app/tests/test_s1_agent_workflow.py` (identity metric, line 133) | `"Registered MCP endpoint"` | `"Registered partner integration endpoint"` | The external IP's owner role was described in internal jargon (content review, S1 identity FIX). The meaning is unchanged: a registered, owned integration endpoint. |
| A3.1 | `backend/app/tests/test_s1_agent_workflow.py::test_s1_run_investigation_concludes_new_mcp_not_malicious` (line 115) | `"mcp" in identity` | `"partner integration endpoint" in identity` | Same rename. The test still asserts the identity step names the registered role. |
| A3.1 | `backend/app/tests/test_s1_agent_workflow.py::test_s1_run_investigation_concludes_new_mcp_not_malicious` (conclusion headline) | `"registered mcp endpoint" in headline` | `"registered partner integration endpoint" in headline` | Same rename as above. |
| A2.3 | `backend/app/tests/test_ec_email_drafts.py::test_s1_firewall_email_includes_sop_and_ticket_sections` | body contains "CONFIRMED FINDINGS" and "Outbound coordination controls" | body contains "WHAT WE KNOW", opens with "ACTION REQUESTED:", and does **not** contain "Outbound coordination controls" or "Experience Center" | Composer rewrite (content review X4): the ask comes first; the send-approval/allowlist footer described console controls to the recipient, and the demo signature leaked. SOP reference, TICKET STATUS, IP and REQUESTED ACTION pins are unchanged. |
| A2.3 | `backend/app/tests/test_ec_cio_lifecycle_walk.py::test_no_demo_words_in_analyst_visible_text` | `xfail(strict=True)` | plain test | Now passes for S1/S2/S4/S7 after the composer and content fixes. |
| A3.1 | `backend/app/tests/test_s1_agent_workflow.py::test_s1_full_agent_lifecycle_to_complete` | remediation step ids = 9-step set | same set + `confirm_owner` | New step asking the integration owner about the 3 unexplained sessions (content review S1 ADD) — the only step that can close the open question. |
| A4.2 | `backend/app/tests/test_e4_flagship_lab_picker.py::test_e4_seven_flagships_and_lab_present` | 7 flagships | 8 flagships (order still equals `FLAGSHIP_SCENARIO_IDS`) | R1 (governed RAG answer) joins the flagship group per plan D2. The lab-set assertions are unchanged. |
| A4.2 | `backend/app/tests/test_ec_pipeline_dispatch_parity.py::test_list_demo_scenarios_leadership_order` | 7 flagships, S1 first | 8 flagships, S1 first, R1 last | Same. |
| A5.1 | `backend/app/tests/test_demo_scenarios_stage3jd.py::test_firewall_incident_exposes_interactive_p1_ticket_action` | a recommended action contains "Open P1 incident record" | contains "escalate INC-2026-89412 to P1" | Storyline (D1): INC-2026-89412 is opened by S1 as P2 on Day 0; Q1 escalates the same incident. The interactive ticket definition (id, priority, group, commander, evidence) pins are unchanged. |
| A5.2 | `backend/app/tests/test_demo_scenarios_stage3jd.py::test_firewall_baseline_template_is_environment_grounded_and_explained` | SPL contains `count(eval(action="deny")) as deny_count` | the base search filters `action=deny`, the SPL contains `count as deny_count` and `hours_observed>=24` | Content review Q2: filter at search time (same count, cheaper), drop the unused event_count, expose the sample size and drop sparse sources. The other structure pins (index, sourcetype, 7-day window, hourly bucket, upper bounds) are unchanged. |
| walkthrough F9 | `frontend/src/components/ec/s1Workspace.test.tsx` (source evidence summary) | "collected from Splunk MCP, SOC-KB / RAG, inventory fixture" | "…, asset inventory" | Visual walkthrough F9: internal "fixture" wording in a visible label. Same three sources are asserted. |
| walkthrough F2 | `test_s1_agent_workflow.py::test_s1_agent_plan_ready_on_initial_turn`, `test_s1_governed_splunk_investigation.py::test_s1_initial_query_asks_last_30_days_not_suspicious_ioc`, `test_s4_zero_day_no_playbook.py::test_s4_agent_plan_ready_on_initial_turn` | plan-stage title states the verdict ("…malicious use not confirmed") / old S4 title | "… — investigation plan ready" | Visual walkthrough F2: the plan stage must not state a verdict nothing has produced yet. Lifecycle, editability and step-count pins are unchanged. |
| walkthrough F1 | `test_s1_governed_splunk_investigation.py::test_s1_initial_journey_titles_are_locked`, `test_s2_ai_application_security.py::test_s2_initial_journey_is_siem_first_reuse_blocked_not_confirmed`, `test_s4_zero_day_no_playbook.py::test_s4_initial_turn_envelope_uses_ten_step_journey`, `test_s7_conflicting_ot_evidence.py::test_s7_initial_journey_lingers_on_conflict` | the first-turn envelope carries the 10-stage architecture journey | the first-turn envelope carries the plan-only journey (header "Preparing the investigation plan"); the unchanged 10-stage pins now run against the `sN_initial()` builders directly | Visual walkthrough F1: the old first-turn animation showed searches executing and findings before the plan was approved. The builders and their locked titles/durations are still tested. |
| walkthrough F9 | `test_s1_agent_workflow.py::test_s1_run_investigation_concludes_new_mcp_not_malicious` | unresolved contains "expected mcp business traffic" | "expected partner business traffic" | Same identity rename as A3.1, applied to the unresolved list. |
| simplify | `frontend/src/components/ec/s1Workspace.test.tsx` (remediation review banner) | "Review remediation plan" | "Review the actions — nothing runs until you approve" | User direction 2026-09-24: less staged UI; the banner is one line. The Approve button and execute-only-after-approve pins are unchanged. |
| simplify | `frontend/src/components/ec/ecCioLayer.test.tsx` | Why line shown whenever a rationale exists; tools strip rendered on the plan | Why line only when `why_visible`; tools strip absent | Same direction: per-step Why lines and the tools strip read as staged. The behaviour is now opt-in per step. |
| simplify | `backend/app/tests/test_s1_agent_workflow.py::test_s1_full_agent_lifecycle_to_complete` | 10 remediation step ids | 3 steps: `generate_spl`, `create_incident`, `notify_firewall` | User direction 2026-09-24: a real plan is 2–3 actions. Validation, backtest, watch window, block decision, owner question and incident update are folded into those steps (`also_applies`), so their follow-ups and evidence still run. |
| simplify | `backend/app/tests/test_s1_agent_workflow.py` (conclusion points; remediation titles) | points contain "agent assessment"; svc_jump_ops asserted in step titles only | points contain "sop"; asserted over title + one-line summary | The conclusion drops the 7-point format (the agent-assessment point repeated the others). svc_jump_ops now sits in the watch step's one-line summary rather than a long title. |
| simplify | `backend/app/tests/test_s1_agent_workflow.py::test_s1_run_investigation_concludes_new_mcp_not_malicious` (result order) | …, permitted_sessions, novelty_window | …, permitted_sessions, retrieve_sop | Novelty is folded into the 30-day check (same search), so it is no longer its own row. The agent-added permitted-sessions step still follows the 30-day check. |
| simplify | `backend/app/tests/test_s1_agent_workflow.py` (detection row; remediation statuses) | `evaluate_notable` row asserted; statuses for `monitor_14d`, `prepare_block`, `deploy_monitoring` | detection result asserted in the conclusion ("didn't fire", "ioc"); `generate_spl` ACTIVE; block decision asserted in `final_summary.deferred` | Those checks and actions are folded into fewer steps, and their outcomes are still asserted where they now appear. |
| simplify | `backend/app/tests/test_s1_agent_workflow.py` (novelty) | `novelty_window` row finding | conclusion says "new in the last 30 days" | Novelty rides the 30-day search; it is stated in the conclusion, not as its own row. |
| simplify | `backend/app/tests/test_s1_agent_workflow.py` (threat intel) | `threat_intel` row finding | conclusion says "threat intel: not listed" (and `check_threat_intel` is still asserted as applied) | TI check folded into the 30-day step. |
| S1 realism | `backend/app/tests/test_s1_*.py` (identity pins) | "(registered) partner integration endpoint" | "partner api endpoint" / "Partner API endpoint (Northwind Logistics)" | User direction: results must read like a real SOC. The IP is now attributed to a named partner (Northwind Logistics, shipment-tracking API). The meaning is unchanged. |
| S1 layout | `frontend/src/components/ec/ecCioLayer.test.tsx` (executive brief) | brief card rendered at outcome and completion | no brief card; `decision_needed` shown with the continue button | User direction: findings then decision then remediation; after remediation only a short summary. |
| R-revamp | `backend/app/tests/test_ec_cio_lifecycle_walk.py` (whole harness) | 5 agent scenarios; S3/S5/S6/Q1 `xfail` "Release B: lifecycle adoption" | all 10 scenarios walked on the spec engine; the xfail becomes a plain `test_every_scenario_is_on_agent_lifecycle` | The xfail was the plan's placeholder for this adoption; it flips exactly as the harness docstring intended. |
| R-revamp | `test_ec_cio_lifecycle_walk.py::test_every_plan_step_says_why` | every step has `rationale`/`decides` (inv) and `rationale`/`reversible`/`approver` (rem) | `test_every_step_says_what_it_does_and_which_tool`: every step has a `summary` and a tool | User direction (S1 review, point 2): remove the "Added because…/why" paragraphs from the plan. What a step does and which connector does it is still required on every step. |
| R-revamp | `test_ec_cio_lifecycle_walk.py::test_executive_brief_at_outcome_and_close` | `executive_brief` with verdict/decision/business impact at outcome and close | `test_priority_and_threat_are_separate_and_policy_based` + `test_final_state_is_not_closed_while_work_is_pending` | The brief card was removed at the user's direction (S1 layout row above). Its substance — a policy-based priority, a threat assessment and a truthful final state — is asserted on the fields that replace it. |
| R-revamp | `test_ec_cio_lifecycle_walk.py::test_s4_default_plan_stops_for_agilus_approval` | S4 investigation pauses at `INVESTIGATION_NEEDS_APPROVAL` for an Agilus scan | `test_s4_uses_agilus_for_versions_and_the_approved_workaround`: Agilus reads the versions in the plan; the device change runs only after approval and is VERIFIED | The mid-investigation pause gated a read. Approval now gates the device change, where it belongs; Agilus stays in S4 (user decision). |
| R-revamp | `test_ec_cio_lifecycle_walk.py::test_s7_default_path_is_live_device_not_recycled_identity` | headline contains "active" | headline contains "still live" | Same finding (device live, not recycled), new wording. |
| R-revamp | `test_ec_cio_lifecycle_walk.py::test_s1_q1_s3_share_one_incident_thread` | S1/Q1/S3 carry one `ec_story_thread` | `test_no_scripted_story_thread` (no thread) + `test_s1_q1_s3_state_the_same_facts` | User direction: no "Day 1 — watch fires". The questions now link through shared facts (incident, IP, host, account), which the facts test asserts. |
| R-revamp | `test_ec_cio_lifecycle_walk.py::test_s1_q1_s3_state_the_same_facts` | "198.51.100.42", "10.20.1.10", "svc_jump_ops", "INC-2026-89412" | "45.xx.xx.42", "JMP-ADM-01", "INC0048213" in all three; svc_netops in Q1/S3; ACL-PARTNER-0147 in S1/S3 | Shared-environment names (masked external IP per user decision; ServiceNow-style ticket). Asserted at COMPLETE because the incident number exists only after it is created. |
| R-revamp | `test_ec_cio_lifecycle_walk.py::test_q1_follow_up_findings_are_visible_and_grow` | Q1 legacy chip `check_identity` appends a finding | removed; Q1's login attribution is the added check asserted by `test_the_agent_adds_a_check_when_evidence_asks_for_it[Q1]` | Q1 is on the agent lifecycle; chips no longer exist in agent mode. |
| R-revamp | `test_ec_cio_lifecycle_walk.py::test_no_demo_words_in_analyst_visible_text` | pattern without "demo" | pattern adds `demo`, `pgcil`, `Northwind`, `198.51.100` | User rule: no DEMO wording, no real-customer index names, no sample company names, no documentation IPs on screen. Strengthened, not weakened. |
| R-revamp | 83 tests listed in `docs/evals/ec_cio_coherence/superseded_tests_r_revamp.txt` (S1–S7, R1 pack tests, SIEM-first envelope tests) | assert the replaced per-scenario pack flows: chip journeys, 10-stage initial journeys, S4 Agilus HIL pause, S5 14→15 upgrade, S6 synonyms, SIEM-coverage/pivot/MITRE envelope fields, old step ids and wording | `@pytest.mark.skip(reason=_SUPERSEDED)` — not deleted, reversible | User directed the revamp of all 10 scenarios onto one realistic flow. The requirements these tests carried are re-asserted on the new engine: nothing runs or sends before approval, no auto-block, validated SPL only with candidate ≠ executed (`test_ec_spec_engine.py`), no forced OT incident before evidence, S6 reuses its incident, no breach claim for blocked attempts, Agilus used for device reads/changes. |
| R-revamp | `test_experience_center_response.py::test_follow_up_advances_turn` | advance via Q1's first chip | advance via `run_investigation` | Agent mode offers no chips; the turn-advance contract is unchanged. |
| R-revamp | `test_ec_agent_framework.py::test_agent_dispatch_returns_none_for_non_agent_scenario` | S3 as the non-agent example | lab scenario `failed_login_spike_app01` | S3 is now an agent scenario; the contract (non-agent → None) is unchanged. |
| R-revamp | `test_ec_agent_framework.py::test_agent_first_turn_initializes_s4_state` | `show_advisory` pre-read applied on turn 1 | plan steps present, nothing applied | The pre-read ran a step before approval; nothing runs before approval now. |
| R-revamp | `test_ec_action_restore_and_query.py::test_suggest_queries_fuzzy_not_exact_only` | a suggestion contains "198.51.100.42" | fuzzy "traffic from external ip to admin systems" suggests the S1 question | Documentation IP removed from visible text; old phrasings still resolve (`test_legacy_phrasings_still_resolve`). |
| R-revamp | `test_r1_rag_answer.py::test_rag_trace_only_after_the_plan_is_approved` | `rag_trace.passages` after run | `investigation_conclusion.sources` after run; no conclusion at plan | User direction: RAG shown as a short cited answer with a Sources line, not a retrieval panel. Still nothing before approval. |
