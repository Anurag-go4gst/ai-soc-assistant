# =============================================================================
# Repo-root .env — SECRETS + PROFILE SELECTOR ONLY
# =============================================================================
# Copy to repo root as .env (gitignored). Full config: env/profiles/<profile>.env.example
# =============================================================================

AI_SOC_ENV_PROFILE=coe

APP_AUTH_ENABLED=true
APP_AUTH_USER=analyst
APP_AUTH_PASSWORD=change-me
APP_AUTH_SESSION_SECRET=change-me-to-random-64-char-hex

# SPLUNK_MCP_TOKEN=
# NVD_API_KEY=
