# Runtime profile drop-in scaffold

Use this folder to store operator-managed runtime profile files (not committed with secrets).

Suggested layout:

- `runtime/coe.env`
- `runtime/development.env`
- `runtime/production.env`

Keep committed templates in `env/profiles/*.env.example`.
