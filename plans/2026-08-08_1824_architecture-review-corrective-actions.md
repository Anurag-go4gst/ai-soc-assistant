---
name: architecture-review-corrective-actions
overview: "Correct the canonical planning architecture review findings, stop specialist-report amplification, establish one tier authority, harden reference qualification, and make all four Resource Planner specialists meaningful without weakening SPL/MCP governance."
status: done
date: 2026-08-08
completed: 2026-08-10
canonical_plan: plans/2026-08-08_1824_architecture-review-corrective-actions.md
implementation_readiness: COMPLETE
final_commit: e5c1937
source_review: docs/architecture/architecture_review_2026-08-08.md
follow_up_audit: plans/2026-08-10_0555_architecture-audit-query-understanding-and-plan-creation.md
---

# Architecture Review Corrective Actions

## Objective

Close the confirmed and corrected findings from the 2026-08-08 canonical-planning architecture review. Done means: the live Resource Planner retains exactly four useful parallel specialists; specialist reports remain bounded and unique; MCP and SPL specialists publish deterministic, redacted plan-readiness reports; one canonical tier vocabulary drives routing and specialist reporting; fuzzy catalogue binding is reconciled into the canonical route without bypassing non-SOC or safety guards; reference qualification has bounded phrase semantics; answer-mode and telemetry policy are explicit; the large canonical lane function is decomposed without behavioural drift; and all targeted, parity, governance, backend, and documentation gates pass with recorded evidence.

This plan is corrective architecture work. It does **not** expand execution authority, enable MCP by default, make candidate SPL executable, or allow specialists to call an LLM/MCP connector.

## Source review corrections carried into this plan

| Review statement | Correct planning premise |
|---|---|
| `match_catalogue_tier()` is report-only | **Superseded by the B1 outcome (2026-08-10).** At authoring time it did feed `app/catalogue/live_router_bind.py`, so the plan treated it as production-authoritative and scoped a migration around it. **Final state: `match_catalogue_tier()` has zero production importers** — only its own definition remains in `app/catalogue/match_tiers.py`; every non-test caller is gone. Production catalogue binding now returns a typed `CatalogueBindingCandidate` via `build_catalogue_binding_candidate()`, and `rp_node_specialist_skill` copies `canonical_planning_input.routing.catalogue_tier` (emitting `canonical_routing_unavailable` when absent) instead of recomputing a tier. Canonical routing is the sole tier authority; the binding candidate is a proposal, never authority. |
| Skill specialist is wrong for every reference-ID query | `Explain CVE-2024-3400` resolves canonically from initial T4 to T0 and already agrees with the T0 specialist report; the confirmed disagreement is reference-ID plus hunt/environment intent, such as `Hunt for T1059 execution in our estate`. |
| `fuzzy_alias_catalog` omission alone explains T3 drift | Alias matching independently returns T3, while the canonical match path can remain T4. The actual problem is the absence of one explicit binding-candidate-to-canonical-route contract. |
| The SPL ladder branch lacks a comment | `pipeline.py` already documents the branch. Only summaries such as `CLAUDE.md` need correction. |
| `run_canonical_planning` is about 690 lines | The seam is 35 lines (unchanged through F0). The 569-line figure for `graph_node_lane_and_canonical_planning` was measured at authoring time and was **stale by execution**: E0 and E1 (`d2b5852`) added to the same function, so the observed pre-refactor size was **613 lines**. F0 decomposed it into four typed stages — `_prepare_planning_intake`, `_resolve_lane_intent_and_details`, `_persist_clarification_outcome`, `_commit_planned_outcome` — leaving a **98-line** seam. |
| No higher-priority runtime defect exists | `specialist_reports` amplifies because every node returns full state into an `operator.add` channel. Re-measured 2026-08-10 via `run_resource_planner_graph` **with a reachable database**: **16,384 reports** on T0 rag_only and **8,192** on T2/T3/T4, against 4 unique lanes. Growth is exponential (doubling per post-merge node), not linear. The earlier "64 / 16x, treat as the floor" reading is **withdrawn**: it was measured without a database, so persistence failed closed and the run took the short `non_planned_finalize` route. Still first priority. |

## Locked decisions

| ID | Decision |
|---|---|
| **D1** | The Resource Planner keeps **exactly four** permanent parallel specialists: `skill`, `knowledge`, `mcp`, and `spl`. MCP and SPL specialists must not be deleted, bypassed, serialized as no-op constants, or folded into another lane. |
| **D2** | Specialists are deterministic advisory/audit components. They may enrich blank arguments on an already-authorized, already-existing Resource Plan step through the validated merge; they may not add steps, change step status, remove policy checks, override non-blank arguments, or authorize execution. |
| **D3** | MCP specialist reads only the committed Evidence/Resource Plan plus redacted/cached MCP registry status. It performs no connector call, discovery network I/O, tool invocation, SPL selection, or execution-gate decision. |
| **D4** | SPL specialist reads only the committed Evidence/Resource Plan, resource registry, template registry, and bounded slot-readiness summaries. It generates no SPL text, calls no LLM, validates no candidate, and always reports `execution_eligible=false`. |
| **D5** | `initial_tier`, `resolved_tier`, and `binding_candidate_tier` are distinct. `catalogue_tier` on canonical/specialist surfaces means the final canonical `resolved_tier`, never a fresh recomputation. |
| **D6** | A validated bounded typo/alias match may promote the **effective canonical match path** to `fuzzy_alias_catalog` only after non-SOC, unsafe-command, exact-authority, and ambiguity guards pass. Parser output is preserved as observed provenance; it is not mutated in place. |
| **D7** | T0 is resolved only by canonical reference qualification. A bare reference-ID regex may extract an ID, but may not independently grant T0. |
| **D8** | `alert_summary` is an evidence-summary/no-SPL family. A contradictory `alert_summary + spl_artifact/spl_generation` contract fails closed as an internal planning contradiction; explicit SPL asks must classify into an SPL-capable family. |
| **D9** | Reference and parity baselines are immutable during ordinary checks. Updating a baseline requires explicit scope and is never the default behaviour of a verification command. |
| **D10** | Existing governance defaults remain: MCP global/server execution default-off; candidate SPL non-executable; only approved non-null `normalized_spl` may reach the MCP gate; LLM advisory-only and never calls MCP. |
| **D11** | The architecture page leads with two explicit active-path diagrams: `T1–T3 → known` and `T4 → reference qualification → T0`. Both planned paths visibly pass through all four specialists and the validated merge before dispatch. Do not communicate a path by dimming most of an all-branches diagram. |
| **D12** | Diagram labels are plain-language first and code identifiers second. Distinguish the pre-plan **Route Skill** (the selected work mode), the post-plan **Routing Contract Auditor** (`specialist_skill`), the **Knowledge Coverage Auditor** (`specialist_knowledge`), and the later **Knowledge Retrieval Worker** (RAG). “Skill” and “Knowledge” may not appear as unexplained duplicate actors. |
| **D13** | Presentation labels use four mutually exclusive dispatch branches: `D1 RAG-only`, `D2 composed step-walk`, `D3 SPL workflow`, and `D4 non-planned stop/finalize`. They are branches after the merge, not sequential stages. Runtime route names remain authoritative; these labels do not create a second dispatch contract. |

## Meaningful specialist report contracts

### MCP specialist — required report surface

The MCP report must remain redacted and bounded. It contains no endpoint, auth mode details beyond safe booleans, credentials, raw tool schemas, SPL, prompt, RAG text, or arbitrary exception strings.

| Field | Meaning / source |
|---|---|
| `plan_needs_mcp` | `EvidencePlan.needs_mcp` plus presence of existing MCP-owned steps. |
| `plan_mcp_allowed` | Copy of `EvidencePlan.mcp_allowed`; capability disclosure only, not execution authorization. |
| `discovery_allowed` | Copy of the bounded discovery policy flag. |
| `planned_hop_count` / compatibility `hop_count` | Count of existing `mcp_execution` / `mcp_discovery` steps, not a constant. |
| `registry_mode` | Safe enum such as `mock` or `registry`. |
| `global_execution_enabled` | Redacted boolean posture only. |
| `configured_server_count`, `available_server_count` | Counts from MCP registry status. |
| `candidate_server_ids` | Bounded safe server identifiers only. |
| `candidate_tool_names` | Bounded intersection of **safe MCP registry candidates** (`load_mcp_registry_status()` safe names, minus blocked), the **committed plan's own step `resource_id`/`purpose`**, and **deterministic capability policy**. It is **not** derived from the output of the fenced legacy multi-hop discovery loop — that lane does not run on the canonical path, and the specialist performs no discovery I/O of its own. These are candidates, never a selected execution tool. (Note the separate, live bounded pre-SPL MCP discovery under dispatch-v2 is a different mechanism and is not an input here either.) |
| `execution_posture` | One of `not_needed`, `discovery_only`, `gate_required`, `blocked_by_plan`, `unavailable`. |
| `requires_execution_gate` | Always true when an execution hop exists. |
| `blockers` / `warnings` | Allowlisted reason codes only. |
| `proposals` | Fill-blank `execution_intent` / safe candidate-name metadata on an existing MCP-owned task only; never query text, normalized SPL, flags, or credentials. |

### SPL specialist — required report surface

The SPL report is planning/readiness metadata only. It never contains SPL text or claims that a candidate passed validation.

| Field | Meaning / source |
|---|---|
| `plan_needs_spl` | `EvidencePlan.needs_spl` plus presence of an existing `spl_artifact` step. |
| `plan_spl_allowed` | Copy of `EvidencePlan.spl_allowed`; not execution authority. |
| `planned_resource_id` | Existing SPL step resource ID. |
| `template_id`, `template_status`, `template_production_executable` | Governed template-registry posture when the plan binds a template. |
| `fallback_resource_id` | Existing Resource Plan `on_unavailable` target, if any. |
| `candidate_source_options` | Ordered bounded enums describing sources the downstream ladder may consider; it does not select or run them. |
| `spl_source` | Meaningful posture enum such as `not_needed`, `governed_template`, `review_only_fallback`, `blocked`, `unavailable`; no generic `template_or_fallback`. |
| `slot_binding_status` | `not_required`, `ready`, `missing_required_slots`, or `unknown`, derived from bounded canonical summaries. |
| `missing_required_slots` | Bounded slot names only, never values. |
| `validation_required` | True for every planned SPL artifact. |
| `execution_eligible` | Hard-coded/validated false on every specialist report. |
| `blockers` / `warnings` | Allowlisted reason codes only. |
| `proposals` | Fill-blank template/fallback/required-slot metadata on an existing `spl_artifact` task only; never SPL text, validator approval, or execution flags. |

## Stop conditions

- All checklist items are checked with observed Evidence and the final re-audit passes, **or**
- The same verification gate fails twice on one item, **or**
- Repo behaviour contradicts a locked decision or a required authority boundary, **or**
- A change would require new execution authority, a new default-on flag, baseline refresh, or a specialist to perform live I/O — stop and ask the user/COE.

Do not adapt silently. Record premise drift in the Drift log and stop before continuing past the affected item.

## Dependency order

`A0 → A1 → B0 → B1 → C0 → D0 → D1 → D2 → D3 → E0 → E1 → F0 → G0 → G1`

## Commit/change-set order

1. Probe tooling only (`A0`).
2. LangGraph state/reducer correctness (`A1`).
3. Canonical tier/binding contract (`B0–B1`).
4. Reference qualification (`C0`).
5. Specialist report contracts and MCP/SPL implementations (`D0–D3`).
6. Answer-mode and telemetry policy (`E0–E1`).
7. Canonical seam extraction (`F0`).
8. Documentation and final gates (`G0–G1`).

Do not combine these groups into one commit. Run `/invariant-check` before every commit that touches planner, pipeline, SPL, or MCP code.

## Checklist

- [x] **P0 — Freeze the pre-change baseline and record the observed starting state**
  - **Do:** Capture the manifest of protected artifacts with `scripts/freeze_execution_baseline.py --capture` (13 artifacts: eval baselines, 105 golden answers, governed registries, and the three published copies of the architecture doc). Then record the *observed* starting numbers in this item's Evidence so every later item is compared against measurement rather than memory: full backend pytest counts, governance regression result, production parity exact/approved/critical, the 10 reference-probe rows, and the current `specialist_reports` cardinality per path. Do not fix anything in this item.
  - **Verify:** `python3 scripts/freeze_execution_baseline.py --capture --out /tmp/exec-baseline.json && python3 scripts/freeze_execution_baseline.py --check --in /tmp/exec-baseline.json && PYTHONPATH=backend:. python3 scripts/run_production_parity_eval.py --out-dir /tmp/arch-corrective-p0-parity --check && ./scripts/run_stage3_governance_regression.sh && cd backend && PYTHONPATH=../backend:.. python3 -m pytest -q`
  - **Depends on:** none
  - **Evidence:** **CLOSED 2026-08-09 on `master@6ec8c9c`.**
    **Manifest:** recaptured `/tmp/exec-baseline.json`, 13 artifacts, immediate re-check clean. The prior `1681f90` manifest was invalidated by three concurrent documentation-only commits (`fa8bc9a`, `3134db3`, `ae0a1f9`); `git diff --stat 1681f90..ae0a1f9` touches only `details.html` (+ its `frontend/public` mirror), the review doc, the plan, and a new test file — **zero backend runtime code**. All ten non-doc protected artifacts verified byte-identical across the range (`reference_knowledge_baseline.md` still `ce142eea3137`); only `published_doc_mirrors` moved, to `45e23fdc56f4`, and all three copies remain mutually identical. Recapture authorised by the user on 2026-08-09; this is not self-inflicted drift because P0 produces no changes.
    **Backend pytest (full, detached):** `2 failed, 4690 passed, 2 skipped, 6 xfailed in 474.68s`.
    **Pre-existing failures (NOT caused by this plan):** `app/tests/integration/test_canonical_retention_purge.py::test_old_eligible_events_removed_recent_events_preserved` and `::test_audit_critical_retention_window_longer_than_diagnostic`. Reproduced in isolation (`2 failed, 12 passed in 5.46s`), so not an ordering or contention artifact. Retention settings are at defaults (purge enabled, diagnostic 7d, audit 90d). The purge SQL skips events whose handoff chain has `expires_at >= now`, but these two tests insert events under a `scope_id` with **no** `canonical_handoffs` row, so the events should be eligible; root cause not yet isolated.
    **Production parity:** `120 exact / 0 approved / 0 critical` (measured 2026-08-08 at `1681f90`; unchanged by docs-only commits).
    **Reference probe rows:** read non-destructively from the frozen baseline (running the script pre-A0 would rewrite it): P1-P4 `knowledge_recall`/`rag_only`/`reference_knowledge`/`reference_taxonomy`; P5 `knowledge_recall`/`live_investigation`/`mitre_knowledge`; P6 + N1 `spl_generation`/`live_investigation`/`spl_authoring`; N2 + N4 `knowledge_recall`/`clarification`; N3 `knowledge_recall`/`live_investigation`/`mitre_knowledge`.
    **specialist_reports cardinality (the A1 defect, re-measured 2026-08-10 — supersedes the earlier 64/16x figure):** **16,384** reports on T0 reference/rag-only and **8,192** on T2 catalogue, T3 fuzzy-alias and T4 guided, against 4 unique `specialist_id`s in every case. Growth is **exponential, not linear**: each post-merge node that returns full state re-appends the whole accumulated list, so the count doubles per node — `4·2^12` and `4·2^11`, matching the 11–12 nodes downstream of `resource_planner_merge`. Mechanism: `Annotated[list, operator.add]` at `resource_planner_graph.py:143` plus every node returning full state via `_record`; masked by `assert len(reports) >= 4` at `test_resource_planner_graph_skeleton.py:118`.
    **Why the earlier 64/16x figure was wrong:** it was measured in a shell without a reachable database. Canonical persistence failed closed (`audit_critical_planning_event_not_persisted`), planning never reached `planned`, and the run took the short `non_planned_finalize` route — only 4 doubling nodes (`4·2^4 = 64`). Confirmed by re-running the identical probe with and without `DATABASE_URL=postgresql://ai_soc:ai_soc_dev_password@127.0.0.1:5434/ai_soc_assistant`: 64 vs 16,384 on the same query. **Any A1 measurement must use the reachable DB endpoint**, or it silently measures a different graph path.
    **Merge-time state is already correct:** on the real path `planner_iteration.reports` and `work_bundle.specialist_reports` both hold exactly 4. All amplification is post-merge, in the governance chain — the fix is the channel reducer alone.
    **Suite is slow, not hung:** 4,700 tests collect in 3.1s and run in ~8min; log grows steadily (~397 bytes/25s). The earlier "stall" at `test_action_lane.py::test_unauthenticated_approval_returns_401_and_executes_nothing` did not reproduce — that test passes in 1.5s alone, 9/9 in its file. Run this suite detached, never inside a short-timeout wrapper.
    **Historical gate blocker:** `scripts/run_stage3_governance_regression.sh:52` runs the full backend pytest, so the governance gate could not pass while the two retention failures stood. It was escalated rather than worked around.
    **Resolution 2026-08-09:** user selected option 1. Commit `757237f` isolates the destructive retention module in a disposable migrated PostgreSQL schema without changing production purge SQL. Verification: retention `14 passed`, full PostgreSQL integration `35 passed`, zero leftover `retention_test_%` schemas, invariant PASS, and all 13 protected artifacts unchanged. P0 remains unchecked until the full governance gate completes green.
    **Current frozen manifest 2026-08-09:** documentation-only commit `3b790c4` preserves the completed architecture animation after the user observed and avoided a torn three-mirror state, waited for 60 seconds of stability, confirmed all copies at SHA256 `d0d4b3453755d2849330e53c1f01a75b9be90a7ed5487dffd69f82ed780ee67c`, and re-validated the final content (valid structure, no duplicate IDs/broken anchors/non-font external references, native SMIL, explicit reduced-motion pause). After fast-forward reconciliation to `master@6ec8c9c`, `/tmp/exec-baseline.json` was freshly captured and checked with no ignore exemption: `protected artifacts unchanged (13 checked)`. The guard exposes no `--ignore` escape hatch; `test_freeze_execution_baseline_cli.py` pins clean capture/check, protected-file drift failure, torn-mirror failure, and rejection of `--ignore` (`4 passed in 0.29s`). The only earlier protected change was the G0-scoped `published_doc_mirrors` update; non-document protected artifacts and runtime code did not move. `arch-t35.png`, `.playwright-mcp/`, and `output/` were excluded from the documentation commit.
    **Final Verify / commit:** production parity `120 exact / 0 approved / 0 critical`; canonical governance regression PASS with backend `4703 passed, 2 skipped, 6 xfailed`, harness `6/6`, clean-answer `120/120`, reference-quality `9/9`, manifest promotion `11/11`, dual-run parity tests `17 passed`, SPL templates `18/18`, Cisco catalogue `50/50`, and dispatch matrix `5/5`; separate detached full backend pytest `4703 passed, 2 skipped, 6 xfailed, 2 warnings in 574.69s`. The first governance launch was non-authoritative because the isolated sandbox deadlocked at 1%; the host marker-file run advanced normally and supplied all recorded results. P0 guard, tests, and evidence committed as `95db1fe`.

- [x] **A0 — Make the reference probe gate non-destructive**
  - **Do:** Extend `scripts/audit_reference_probes.py` with a default non-mutating `--check` mode that loads and compares the frozen route rows while ignoring generated timestamps; add explicit `--out <path>` for scratch reports and an explicit `--update-baseline` operation that is never invoked by this plan. Add `backend/app/tests/test_reference_probe_audit_cli.py` (**NEW**) covering pass, drift/non-zero exit, scratch output, and unchanged baseline bytes. Do not refresh `docs/evals/reference_knowledge_baseline.md`.
  - **Verify:** Run the probe against the established host-reachable PostgreSQL endpoint while preserving the production canonical-persistence path. Capture the probe exit separately so drift cannot prevent the post-run hash from being recorded; accept only the documented comparison exits (`0` match or `1` drift), require all ten rows to have reached a `PASS`/`DRIFT` comparison result, and independently prove the baseline bytes are unchanged. Exact command: `cd /var/www/ai-soc-assistant && bash -c 'set -eu; sha256sum docs/evals/reference_knowledge_baseline.md > /tmp/reference-baseline-before.sha256; set +e; DATABASE_URL=postgresql://ai_soc:ai_soc_dev_password@127.0.0.1:5434/ai_soc_assistant TELEMETRY_MODE=none PYTHONPATH=backend:. python3 scripts/audit_reference_probes.py --check > /tmp/reference-probe-check.log 2>&1; probe_rc=$?; set -e; sha256sum docs/evals/reference_knowledge_baseline.md > /tmp/reference-baseline-after.sha256; diff -u /tmp/reference-baseline-before.sha256 /tmp/reference-baseline-after.sha256; case "$probe_rc" in 0|1) ;; *) cat /tmp/reference-probe-check.log; exit "$probe_rc" ;; esac; test "$(grep -Ec "^[[:space:]]+(P[1-6]|N[1-4])[[:space:]]+(PASS|DRIFT)" /tmp/reference-probe-check.log)" -eq 10' && cd backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_reference_probe_audit_cli.py -q`
  - **Depends on:** P0
  - **Evidence:** **VERIFY PASSED 2026-08-09; item remains unchecked pending P0 completion, concurrent-session clearance, and the A0-only commit.** The corrected wrapper completed with exit `0`; the live `--check` comparison correctly returned drift (`1`) after producing all ten verdict rows, and the CLI suite passed `11 passed in 1.28s`. The baseline before/after SHA256 values were byte-identical at `ce142eea3137ad71d0bc679387f6a45d595415c25d6b8ce41ed8a965b39b9021`; the refreshed protected manifest also checks clean (`13 checked`). Rows P1, P2, P3, P4, P6, and N3 PASS. Rows P5, N1, N2, and N4 DRIFT and are pinned as C0 input below. Earlier combined coverage also passed (`test_reference_probe_audit_cli.py` plus the three downstream utility SPL tests: `14 passed`), proving the script import no longer leaks offline settings into later tests. The earlier database failures correctly proved that audit-critical canonical persistence fails closed when the Docker-only `postgres` hostname is unreachable; the successful gate used the established host endpoint (`127.0.0.1:5434`) and did not disable persistence. The detached completion waiter is not evidence: its `pgrep -f` expression matches its own command line and remains asleep after the real probe/test processes exit.
    **Independently re-confirmed 2026-08-09 (this session):** wrapper exit `0`; CLI/unit `11 passed`; all ten rows compared, **6 PASS (P1 P2 P3 P4 P6 N3) / 4 DRIFT (P5 N1 N2 N4)**; baseline `ce142eea3137ad71d0bc` identical before and after and still that value on disk. The deliverable is therefore *proven*, not merely implemented: the baseline survived a run that reported drift — previously impossible, because the script had no argparse and called `OUT.write_text()` unconditionally, making the gate unable to fail.
    **Blockers now cleared:** the retention failures were fixed in `757237f`; the concurrent doc writer was caught mid-write (hash moved `dbad9916` → `f4e43be6` → `d0d4b345` across ~90s, with `docs/` briefly out of sync with its mirrors), then stabilised, re-validated and committed as `3b790c4`; the manifest was re-captured against that commit and re-checks clean at 13/13.
    **Waiter defect noted:** the detached `until ! pgrep -f "audit_reference_probes…"` waiter never exits because `pgrep -f` matches the waiter's own command line. Same self-match killed a follow-up `pkill -f` invocation (exit 144). Use a marker file or `pgrep -f` with a pattern that cannot match the watching shell.

- [x] **A1 — Stop additive specialist-report amplification**
  - **Do:** Replace the raw `operator.add` channel semantics for `ResourcePlannerGraphState.specialist_reports` with a deterministic, idempotent reducer keyed by `delegation_id` and `specialist_id`. Identical replay is deduplicated; conflicting reports for one delegation fail closed. Preserve the four-way `Send` fan-out. Add `backend/app/tests/test_resource_planner_specialist_report_cardinality.py` (**NEW**) and strengthen `test_resource_planner_graph_skeleton.py` from `>= 4` to exactly four unique reports in final state, `PlannerIteration`, and `WorkBundle` for T0, T1/T2, T4 guided, and fuzzy-alias probes.
    **Also add the four fan-out edges to `_documented_resource_planner_edges()`.** They are genuinely absent from `resource_planner_graph_edges()` today — verified 2026-08-10. That helper exists precisely for "static edges LangGraph `Send` fan-out does not surface via `get_graph()`" (its own docstring) and documents `route_setup → resource_planner_delegate` and `specialist_* → resource_planner_merge`, but omits `resource_planner_delegate → specialist_*` — so the fan-out itself, the one thing the helper was written for, is the one thing unpinned. Adding it is a contract fix, not a test accommodation.
  - **Anti-regression pins:** the new test must assert **exactly** 4 reports and 4 unique `specialist_id`s (observed: 16,384 on T0 rag_only; 8,192 on T2/T3/T4) — a `>=` assertion is what let this defect live, so it must not reappear. It must also assert that the *content* of each surviving report is unchanged, and that all four `Send` branches appear in `resource_planner_graph_edges()`
    **Content-parity method (corrected 2026-08-10):** compare against the **payload captured at fan-out**, not against specialists re-invoked on the final state. Re-invoking on final state measures a different input — the Knowledge specialist's fill-blank proposal has already been applied downstream by then, so its rebuilt report legitimately differs. That was a defect in this pin, not in the code.
    **Scope of the parity assertion:** full content parity applies to the final-state `specialist_reports` channel, which is what the reducer governs. For `PlannerIteration.reports` and `WorkBundle.specialist_reports`, assert **cardinality (exactly 4), unique `specialist_id`s, and base-`SpecialistReport` field parity only** — those models declare `list[SpecialistReport]`, so Pydantic narrows subclass instances and drops `skill_id`/`catalogue_tier`/`hop_count`/`reference_domains`/`spl_source`. That narrowing is pre-existing, identical before and after the reducer, and **has no production reader** (verified 2026-08-10: the sole consumer, `_knowledge_reference_domains` at `pipeline.py:9532`, reads `reference_domains` from step `args_template`, not from the report). Do not widen those models inside A1 — see A2. (this assertion fails until the `_documented_resource_planner_edges()` fix above lands).
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_resource_planner_specialist_report_cardinality.py app/tests/test_resource_planner_graph_skeleton.py app/tests/test_planner_hierarchy_contracts.py app/tests/test_resource_planner_validated_work_bundle.py -q && cd /var/www/ai-soc-assistant && python3 scripts/freeze_execution_baseline.py --check --in /tmp/exec-baseline.json`
  - **Depends on:** A0
  - **Evidence:** **BLOCKED — failing-first premise contradiction; no runtime implementation or A1 commit.** The user confirmed the parallel documentation session ended; `master` was reconciled and the freshly captured manifest checked clean for all 13 protected artifacts with no ignore exemption. New failing-first `test_resource_planner_specialist_report_cardinality.py` then observed final-state cardinality of **16,384** for T0 reference/rag-only and **8,192** for T2 catalogue, T3 fuzzy-alias SPL, and T4 guided paths—not the locked 64/16x premise. It also proved all four `resource_planner_delegate → specialist_*` Send branches are absent from `resource_planner_graph_edges()`. Result: `5 failed in 2.33s` (four exact-cardinality failures plus the Send-edge failure). The test asserts exact four reports/unique IDs, raw per-lane content equality, exact four reports in `PlannerIteration` and `WorkBundle`, and both sides of all four fan-out/fan-in branches. Because the measured starting magnitude is contradicted, execution stopped before changing `resource_planner_graph.py`; B0/B1 were not started.
    **Resume attempt 2026-08-10 — BLOCKED on a second corrected-premise contradiction after one A1 Verify failure.** Required-DB failing-first measurement reproduced `16,384 / 8,192 / 8,192 / 8,192` and four missing delegate fan-out edges (`5 failed`). Canonical-JSON inventory then proved every amplified replay is byte-identical within each `(delegation_id, specialist_id)` key (exactly one distinct value per key on all four paths), authorizing fail-closed keyed reduction. The scoped reducer and documented edges reduce final state to exactly four and pass the topology/cardinality checks, but the unchanged content assertion exposes two pre-existing transformations that the revised premise said would pass: (1) rebuilding `rp_node_specialist_knowledge(state)` from final state intentionally omits the original fill-blank proposal because `_apply_work_bundle_to_workers()` has already applied it to `evidence_plan`; (2) `PlannerIteration.reports` and `WorkBundle.specialist_reports` are typed as `list[SpecialistReport]`, so Pydantic serialization drops subtype fields (`catalogue_tier`/`skill_id`, `reference_domains`, `hop_count`, `spl_source`). A1 Verify result: `4 failed, 28 passed`; exact cardinality, unique IDs, replay-idempotence/conflict tests, and all four Send-edge assertions pass. No second Verify was run and no commit was made. Passing the unchanged post-hoc builder assertion would require weakening Knowledge fill-blank semantics or caching an old report and thereby hiding changed-input conflicts; execution stopped instead. The correct test design must capture the original fan-out payload (or pin reducer identity directly) and compare final/iteration/bundle payloads against that immutable value, while production models preserve subtype content.
    **CLOSED 2026-08-10 after user adjudication:** the test now captures all four reports at the actual pre-fan-in delegate state, proves exact full-payload equality against final-state `specialist_reports`, and limits iteration/bundle checks to exact cardinality, four unique IDs, and base-report parity; A2 owns the pre-existing subtype narrowing. Required-DB A1 Verify: `32 passed, 1 warning in 5.71s`; manifest `13 checked`, unchanged. Explicit reducer unit pins prove identical replay idempotence and conflicting same-key payload rejection. Wider risk gates also passed: production parity `120 exact / 0 approved / 0 critical`; canonical governance regression PASS with full backend `4714 passed, 2 skipped, 6 xfailed, 2 warnings in 566.94s`, dual parity `120/0/0`, clean answers `120/120`, SPL templates `18/18`, Cisco catalogue `50/50`, and dispatch `5/5`. No conflict exception or order-sensitive drift surfaced. An initial non-authoritative governance launch incorrectly set `TELEMETRY_MODE=none` and produced five telemetry-default failures; all five passed immediately with the invalid override removed, and the complete default-posture rerun is the recorded gate.

- [x] **A2 — Preserve specialist subtype fields through PlannerIteration/WorkBundle (deferred out of A1)**
  - **Do:** `WorkBundle.specialist_reports` and `PlannerIteration.reports` are typed `list[SpecialistReport]`, so Pydantic narrows `SkillSpecialistReport`/`McpSpecialistReport`/`KnowledgeSpecialistReport`/`SplSpecialistReport` to the base model and silently drops `skill_id`, `catalogue_tier`, `hop_count`, `reference_domains`, and `spl_source`. Replace the annotation with a discriminated union on `specialist_id` so subtype fields survive serialization.
  - **Why this is not A1:** the narrowing is pre-existing and unchanged by the reducer, and no production code reads those fields off the bundle or iteration — verified 2026-08-10, the only consumer (`_knowledge_reference_domains`, `pipeline.py:9532`) reads `reference_domains` from step `args_template`. It is a modeling cleanup, not a correctness fix, and folding it into an amplification fix would mix two blast radii.
  - **Governance note:** widening these models pushes previously-dropped fields into whatever consumes the bundle/iteration downstream, including telemetry and trace surfaces. D3 requires the MCP report to stay redacted and bounded, and `catalogue_tier` on specialist surfaces is governed by D5. Re-check redaction on every newly-surfaced field before landing; this item is a candidate to merge into B0 rather than run standalone.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_planner_hierarchy_contracts.py app/tests/test_resource_planner_validated_work_bundle.py app/tests/test_resource_planner_specialist_report_cardinality.py -q` plus a redaction review of any field newly reaching a telemetry surface.
  - **Depends on:** A1
  - **Evidence:** **CLOSED 2026-08-10.** Replaced both base-class report lists with a `specialist_id`-discriminated union and strengthened the four-path cardinality test to require full subtype payload parity in final state, `PlannerIteration`, and `WorkBundle`. Exact Verify passed (`18 passed`, exit 0); review found no production reader beyond the validated work-bundle seam and no newly preserved endpoint, credential, prompt, RAG-text, or raw-SPL field. Protected manifest remains 13/13 clean.

- [x] **B0 — Pin the canonical tier and binding-candidate contract with failing-first tests**
  - **Do:** Add `backend/app/tests/test_canonical_catalogue_tier_authority.py` (**NEW**) defining `initial_tier`, `resolved_tier`, `catalogue_tier`, `binding_candidate_tier`, parser `observed_match_path`, and canonical `effective_match_path`. Cover: pure CVE definition `T4→T0`; T1059 hunt stays `T4`; exact row T1; catalogue row T2; accepted bounded typo alias becomes effective `fuzzy_alias_catalog`/T3; non-SOC, unsafe action, ambiguous alias, and exact-authority negative controls are never promoted. Pin Skill specialist equality to canonical `catalogue_tier`.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_canonical_catalogue_tier_authority.py -q && cd /var/www/ai-soc-assistant && python3 scripts/freeze_execution_baseline.py --check --in /tmp/exec-baseline.json`
  - **Depends on:** A1
  - **Evidence:** **CLOSED 2026-08-10.** Added `test_canonical_catalogue_tier_authority.py`; required-DB failing-first run produced `9 failed` — eight probe IDs stopped on the missing `catalogue_binding_candidate`/effective-path contract, and the corrected production-only AST inventory found the two legacy classifier importers `catalogue/live_router_bind.py` and `graph/resource_planner_graph.py`. Final matrix passes `10/10`: CVE definition `T4→T0`; T1059 hunt remains `T4`; exact/T2/accepted-alias routes resolve `T1/T2/T3`; non-SOC, unsafe, and ambiguous alias candidates remain effective `out_of_registry/T4`; Skill report equals canonical `catalogue_tier`; missing canonical routing yields `catalogue_tier=None` plus `canonical_routing_unavailable`. Manifest remains 13/13 clean.

- [x] **B1 — Establish one production tier authority and reconcile fuzzy aliases**
  - **Do:** Refactor `app/catalogue/match_tiers.py` / `live_router_bind.py` so production catalogue binding returns a typed binding candidate rather than independently granting T0–T4. Introduce a declared `effective_catalogue_match_path` state channel; preserve the parser path separately as observed provenance. Apply D6 guards before accepting `fuzzy_alias_catalog`; consume the effective path in canonical lane construction. Make `rp_node_specialist_skill` copy `canonical_planning_input.routing.catalogue_tier` and emit a warning/`None` when canonical routing is unavailable—never recompute. Keep a compatibility wrapper only while callers/tests are migrated; assert no production module outside the canonical binding seam imports the legacy tier classifier.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_canonical_catalogue_tier_authority.py app/tests/test_catalogue_bind_surface_agreement.py app/tests/test_catalogue_match_tiers.py app/tests/test_live_catalogue_router_probes.py app/tests/test_resource_planner_dry_runs.py app/tests/test_state_channel_parity.py app/tests/test_dual_runtime_lane_parity.py -q && cd /var/www/ai-soc-assistant && PYTHONPATH=backend:. python3 scripts/run_production_parity_eval.py --out-dir /tmp/architecture-corrective-tier-parity --check`
  - **Depends on:** B0
  - **Evidence:** **CLOSED 2026-08-10.** `build_catalogue_binding_candidate()` now proposes only T1–T4; `match_catalogue_tier()` remains in `match_tiers.py` as a compatibility/test wrapper and still owns no production authority. `live_router_bind.py` returns a typed candidate with `binding_candidate_tier`, observed/candidate/effective paths, acceptance, and an allowlisted decision reason; fuzzy promotion occurs only after exact-authority, non-SOC, unsafe/run, ambiguity, and existing-selected-use-case precedence guards. `ChatPipelineState` declares candidate/observed/effective channels; canonical `RoutingContext.match_path` follows the effective path while preserving `observed_match_path`, and `catalogue_tier` equals final `resolved_tier`. `rp_node_specialist_skill` copies that canonical tier and never recomputes. Production import inventory for `match_catalogue_tier`: **zero**; `match_tiers.py` was not deleted or relocated. B1 targeted Verify: `45 passed, 1 warning in 12.28s`. Final marker-backed parity: `120 exact / 0 approved / 0 critical`; manifest `13 checked`, unchanged. One earlier parity invocation was mistakenly left running because its wrapper hid the session ID; the exact duplicate PID was terminated and only the marker-backed exit-0 run supplies evidence.

- [x] **C0 — Correct bounded reference qualification and marker semantics**
  - **Do:** In `app/chat/reference_qualification.py`, replace raw substring scans with a shared boundary-aware phrase matcher **consistently across all five marker groups, including action and investigation markers**. Replace bare environment-status words (`vulnerable`, `exposure`) with explicit phrases (`are we vulnerable`, `is our environment vulnerable`, `our exposure`, etc.). Use only allowlisted deterministic signals (`explicit_log_search`, `live_data_request`, `block_or_contain`, `run_execution`) to deny a knowledge-only T0 short circuit; an ID regex only extracts IDs. Extend existing reference tests with `four hours`, `hour ago`, `your team`, `flour`, definitional vulnerable-component wording, and positive environment/live-search controls. Preserve P1–P6/N1–N4 frozen behaviour through `--check`.
  - **A0 drift input:** The first valid non-mutating comparison produced four pre-C0 drifts: P5 lost `answer_mode=live_investigation`, `request_mode=mitre_knowledge`, `stage_schedule=[mitre_finalize]`, and its MITRE panel; N1 lost only the terminal `mcp_execution` stage; N2 and N4 lost their `clarification` answer/request modes. P1, P2, P3, P4, P6, and N3 already match. Diagnose and correct those four rows here; do not refresh the frozen reference baseline to absorb them.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/ -k "reference_qualification or reference_registry or answer_shape" -q && cd /var/www/ai-soc-assistant && DATABASE_URL=postgresql://ai_soc:ai_soc_dev_password@127.0.0.1:5434/ai_soc_assistant TELEMETRY_MODE=none PYTHONPATH=backend:. python3 scripts/audit_reference_probes.py --check`
  - **Depends on:** B1
  - **Evidence:** **CLOSED 2026-08-10 after user/COE adjudication.** Boundary-aware matching covers all five marker groups and direct controls pass for `four hours`, `hour ago`, `your team`, `flour`, definitional vulnerable wording, bounded action/investigation substrings, explicit environment phrases, and allowlisted live signals. The first two valid host-DB audits remain recorded as **6 PASS / 4 DRIFT** and **9 PASS / 1 DRIFT** (`P5 has_mitre_panel: True -> False`). The adjudicated correction preserves P5's frozen panel through existing MITRE status policy: T1110/T1110.001 are `requires_validation`, T1110.003 is `candidate`, no mapping is supported/confirmed/evidence-supported, evidence keys and collected-evidence count remain zero, `allow_mitre_mapping=false`, and execution remains skipped. Final exact selector: `46 passed, 4705 deselected`; additional dispatch/MITRE slice: `42 passed`; frozen audit: **10 PASS / 0 DRIFT**. Invariant check PASS; protected manifest `13 checked`, unchanged; the frozen reference baseline was not modified or refreshed.

- [x] **D0 — Expand specialist report contracts and proposal safety**
  - **Do:** Extend `McpSpecialistReport` and `SplSpecialistReport` in `app/planner/planner_hierarchy.py` with the required fields defined above while preserving compatibility fields. Add enum/bounds validators and forbidden-key validation. Strengthen `apply_specialist_reports()` to fill blanks only, reject cross-lane purposes, reject non-existing step proposals, and forbid specialist proposals from setting `candidate_spl`, `normalized_spl`, validator approval, `execution_enabled`, `execution_eligible`, policy checks/status, endpoint/auth/secret fields, or raw query/prompt/RAG content. Add `backend/app/tests/test_specialist_report_contracts.py` (**NEW**).
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_specialist_report_contracts.py app/tests/test_planner_hierarchy_contracts.py app/tests/test_resource_planner_validated_work_bundle.py app/tests/test_decision_record.py -q && cd /var/www/ai-soc-assistant && python3 scripts/freeze_execution_baseline.py --check --in /tmp/exec-baseline.json`
  - **Depends on:** C0
  - **Evidence:** **CLOSED 2026-08-10.** Expanded MCP/SPL reports with typed posture/source/slot enums, bounded counts/hops/lists, safe identifier validation, synchronized compatibility hop counts, and hard `execution_eligible: Literal[False]`. The merge rejects candidate/normalized SPL, validator approval, execution flags, policy/status mutation, endpoint/auth/credential/secret/token fields, and raw query/prompt/RAG content (including nested keys); it also rejects cross-lane, nonexistent, ambiguous, and non-blank overwrite proposals. Additive tests prove blank-only enrichment preserves plan status and policy checks. Exact Verify: `40 passed, 1 warning`; protected manifest `13 checked`, unchanged.

- [x] **D1 — Implement the meaningful deterministic MCP specialist**
  - **Do:** Add `app/planner/mcp_specialist.py` (**NEW**, matching `knowledge_specialist.py` structure) with a pure `build_mcp_audit_report()` that reads the committed plan, redacted `load_mcp_registry_status()`, resource registry, and bounded capability metadata. Derive planned hop count, plan/discovery permissions, safe server/tool candidates, execution posture, blockers, and fill-blank proposals exactly per D3 and the MCP contract table. `candidate_tool_names` must come from safe registry candidates, committed plan purpose, and deterministic capability policy—never from output of the fenced legacy multi-hop discovery loop. Preserve `planned_hop_count` for genuine `mcp_execution` steps. It must not call `select_mcp_tool`, any connector/discovery lifecycle, or the execution gate. This specialist boundary does **not** mean all MCP discovery is absent: legacy Resource Planner multi-hop discovery/observer flow is fenced, while bounded pre-SPL MCP discovery is a separate dispatch-v2 mechanism. Wire `rp_node_specialist_mcp` to this builder. Add `backend/app/tests/test_mcp_specialist_audit.py` (**NEW**) with not-needed, discovery-only, gate-required, plan-blocked, unavailable, blocked-tool, redaction, no-live-I/O, and merge cases.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_mcp_specialist_audit.py app/tests/test_specialist_registry.py app/tests/test_mcp_tool_planner.py app/tests/test_mcp_execution_gate.py app/tests/test_resource_planner_specialist_report_cardinality.py -q`
  - **Depends on:** D0
  - **Evidence:** **CLOSED 2026-08-10.** Added pure `build_mcp_audit_report()` and wired the permanent MCP lane to it. Tests cover all postures: empty plan → `not_needed`; committed metadata step → `discovery_only`; allowed `mcp_execution` step → `gate_required`; the same step with `mcp_allowed=false` → `blocked_by_plan`; unavailable registry/no safe candidate → `unavailable`. Genuine execution and discovery steps preserve `planned_hop_count`/`hop_count`; candidates are the bounded intersection of available registry safe names, the committed step resource/purpose, and deterministic capability policy, so injected legacy discovery output is ignored. Injected registries prove loader/connector/discovery/selection call count zero; redaction excludes transport/auth/error details, secrets, SPL, and query payloads. Exact Verify: `54 passed, 1 warning`.

- [x] **D2 — Implement the meaningful deterministic SPL specialist**
  - **Do:** Add `app/planner/spl_specialist.py` (**NEW**) with a pure `build_spl_audit_report()` that reads the committed plan, SPL/resource registries, canonical use-case/template provenance, and bounded slot/source-profile summaries. Derive template/fallback/source options, slot readiness, validation requirement, meaningful `spl_source`, blockers, and fill-blank proposals per D4 and the SPL contract table. Hard-validate `execution_eligible=false`; include no SPL text and call no LLM/renderer/validator. Wire `rp_node_specialist_spl` to this builder. Add `backend/app/tests/test_spl_specialist_audit.py` (**NEW**) for not-needed, active template, inactive/missing template, lab-review fallback, missing slots, source-profile gaps, forbidden SPL text, and merge cases.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_spl_specialist_audit.py app/tests/test_specialist_registry.py app/tests/test_spl_generation_stage.py app/tests/test_resource_planner_specialist_report_cardinality.py -q`
  - **Depends on:** D1
  - **Evidence:** **CLOSED 2026-08-10.** Added pure `build_spl_audit_report()` and wired the permanent SPL lane to it. Source matrix: no SPL step → `not_needed`; active production template → `governed_template`; `skill:spl_generation` or available lab fallback → `review_only_fallback`; denied plan → `blocked`; missing/inactive resource/template → `unavailable`. Slot matrix reports `not_required`, `ready`, `missing_required_slots`, or `unknown` from bounded normalized/source-profile summaries and emits slot names only. Tests inject a template containing SPL/secret-like text and prove neither escapes; injected registries prove no loader, LLM, renderer, or validator call; every report validates `execution_eligible=false`. Exact Verify: `27 passed, 1 warning`.

- [x] **D3 — Prove four-lane fan-in contributes useful, bounded information**
  - **Do:** Add end-to-end Resource Planner assertions that every planned turn yields exactly one report per permanent specialist; MCP/SPL constants are absent; useful fields agree with the committed Evidence/Resource Plan; only owned blank arguments are enriched; reports are bounded and redacted; `WorkBundle` parity prevents step/policy/status mutation. Update stable decision records to summarize posture with allowlisted codes rather than copying full reports. No specialist report becomes a new execution input outside the validated WorkBundle merge.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_resource_planner_specialist_report_cardinality.py app/tests/test_resource_planner_graph_skeleton.py app/tests/test_resource_planner_validated_work_bundle.py app/tests/test_mcp_specialist_audit.py app/tests/test_spl_specialist_audit.py app/tests/test_knowledge_specialist_audit.py -q`
  - **Depends on:** D2
  - **Evidence:** **CLOSED 2026-08-10.** The T0 reference, T2 catalogue, T3 fuzzy-alias, and T4 guided paths each produce exactly four unique reports (`skill`, `knowledge`, `mcp`, `spl`) with full fan-out→iteration→WorkBundle parity. MCP hop/gate fields and SPL planned resource/source fields agree with the committed source plan; constants `mcp_lane_advisory`, `spl_lane_advisory`, and `template_or_fallback` are absent. Every serialized report is bounded to ≤8 KiB with list fields ≤16 and no SPL, endpoint/auth/secret, raw-query/prompt, or RAG-content keys. WorkBundle tasks preserve the source plan's exact step set, statuses, and policy checks; only owned blank args change. Stable specialist decision records carry only allowlisted posture reason codes and empty payloads. Exact Verify: `51 passed, 1 warning`. D0–D3 invariant check PASS; production parity `120 exact / 0 approved / 0 critical`; governance regression PASS with backend `4774 passed, 2 skipped, 6 xfailed`, harness `6/6`, clean answers `120/120`, SPL templates `18/18`, Cisco catalogue `50/50`, and dispatch `5/5`. Protected manifest remains `13 checked`, unchanged.

- [x] **E0 — Replace implicit answer-mode conditionals with an explicit ordered policy**
  - **Do:** Introduce an inspectable ordered answer-mode policy covering lane, answer goal, and intent family; do not reduce it to a family-only map. Preserve the current precedence for clarification, reference/knowledge, SPL, guided, alert-summary, and planner-decides cases. Encode D8: alert-summary remains `rag_only`, while contradictory alert-summary/SPL goals produce a typed fail-closed canonical planning error rather than silently opening a SPL lane. Add `backend/app/tests/test_canonical_answer_mode_policy.py` (**NEW**) and retain regression coverage for the prior catch-all bug.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_canonical_answer_mode_policy.py app/tests/test_canonical_planning_architecture.py app/tests/test_out_of_registry_security_log_investigation_routing.py app/tests/test_routing_summary_investigation.py app/tests/test_canonical_outcome_gate.py -q && cd /var/www/ai-soc-assistant && python3 scripts/freeze_execution_baseline.py --check --in /tmp/exec-baseline.json`
  - **Depends on:** D3
  - **Evidence:** **CLOSED 2026-08-10.** Added the inspectable ordered matrix `clarification -> alert_summary_spl_contradiction -> reference_or_knowledge -> spl -> guided -> alert_summary -> planner_decides`, with rule metadata identifying the lane, answer-goal, intent-family, and clarification dimensions each rule reads. Failing-first coverage pins clarification precedence, T0/reference `rag_only`, SPL `live_investigation`, guided investigation, alert-summary `rag_only`, and the prior unknown-family catch-all regression. A contradictory `intent_family=alert_summary` plus `answer_goal=spl_generation|spl_artifact` raises the typed policy error and the canonical orchestrator converts it to `planning_failed` with an empty dispatch schedule and no EvidencePlan, execution, or MCP evidence. Exact Verify: `37 passed`; protected manifest `13 checked`, unchanged.

- [x] **E1 — Fail closed on unclassified canonical telemetry events**
  - **Do:** Require every `emit_planning_event()` event to exist in the canonical telemetry catalog before it is logged or persisted. Add a static inventory test that discovers literal production emissions and compares them to the 8 audit-critical + 20 diagnostic catalog entries; unknown, duplicate-classified, and unclassified events fail. Preserve audit-critical persistence semantics and diagnostic degradation behaviour. Add/extend tests in `test_canonical_telemetry_coverage.py` and `test_telemetry_persistence_policy.py`.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_canonical_telemetry_coverage.py app/tests/test_telemetry_persistence_policy.py app/tests/integration/test_canonical_retention_purge.py -q && cd /var/www/ai-soc-assistant && python3 scripts/freeze_execution_baseline.py --check --in /tmp/exec-baseline.json`
  - **Depends on:** E0
  - **Evidence:** **CLOSED 2026-08-10.** The closed classifier preserves the locked **8 audit-critical / 20 diagnostic** partition and rejects empty, unknown, and duplicate-classified event names. `emit_planning_event()` now classifies before allocating a decision id or appending to the memory log, and direct durable persistence classifies before sanitizing, buffering, capturing, or writing. A static AST inventory discovers direct literal emissions plus bounded detail-tool, execution, and execution-step wrapper emissions and equals the 28-event catalog exactly. Unknown-event tests prove both in-memory and persisted stores remain empty; audit-critical persistence and diagnostic degradation semantics remain green. Unit slice: `29 passed`; host-reachable PostgreSQL retention integration: `14 passed`; corrected DB-unit/catalog slice: `34 passed`. The first full governance attempt validly found two DB unit-of-work tests using synthetic suffixed event names; those tests now repeat real catalog events while retaining their connection-budget assertions. One subsequent launch omitted the mandated host DB URL and is excluded as an operationally invalid run. Final host-DB governance PASS: backend `4788 passed, 2 skipped, 6 xfailed`, harness `6/6`, dual parity `120/0/0`, clean answers `120/120`, SPL templates `18/18`, Cisco catalogue `50/50`, and dispatch `5/5`. E0-E1 invariant check PASS; protected manifest `13 checked`, unchanged.

- [x] **F0 — Decompose the 569-line canonical lane function without behavioural drift**
  - **Do:** Refactor `graph_node_lane_and_canonical_planning` into named typed stages for handoff/resume preparation, lane+intent/detail resolution, canonical input/policy outcome construction, and plan persistence/commit. `run_canonical_planning` remains the sole shared entry seam, `plan_evidence_from_canonical` remains the sole plan creator, and no new orchestration flag or planning fork is introduced. Add static negative-control coverage that both production runtimes and shadow wrapper still call the same seam and no extracted stage independently composes a Resource Plan.
  - **Verify:** `cd /var/www/ai-soc-assistant/backend && PYTHONPATH=../backend:.. python3 -m pytest app/tests/test_dual_runtime_single_orchestration.py app/tests/test_canonical_architecture_complete.py app/tests/test_canonical_planning_architecture.py app/tests/test_resource_plan_authority.py app/tests/test_canonical_handoff_e2e_probes.py -q && cd /var/www/ai-soc-assistant && PYTHONPATH=backend:. python3 scripts/run_production_parity_eval.py --out-dir /tmp/architecture-corrective-seam-parity --check`
  - **Depends on:** E1
  - **Evidence:** **CLOSED 2026-08-10.**
    **Anchor correction:** the target function measured **613 lines**, not 569 — E0/E1 (`d2b5852`) added to it after the plan was authored. `run_canonical_planning` is unchanged at 35 lines, so the seam claim in the premise table still holds.
    **Extracted stages** (all in `app/chat/canonical_planning_orchestrator.py`, typed via two frozen dataclasses `_PlanningIntake` / `_LaneResolution`): `_prepare_planning_intake` (128) — query/handoff-resume preparation and lane routing; `_resolve_lane_intent_and_details` (348) — the resume / known / T4 three-branch intent+detail resolution and post-guided completeness; `_persist_clarification_outcome` (87); `_commit_planned_outcome` (117). `graph_node_lane_and_canonical_planning` is now a **98-line** seam that builds the canonical input, evaluates the policy boundary, and dispatches to clarification-persist or plan-commit. Terminal states propagate through a `terminal_state` field rather than by reordering control flow, so the three original early-return points are preserved exactly.
    **No new fork or flag:** `run_canonical_planning` untouched; `plan_evidence_from_canonical` remains the sole plan creator, reached from exactly one stage.
    **Negative controls added** to `app/tests/test_dual_runtime_single_orchestration.py` (7 new tests): each stage is asserted free of `compose_resource_plan` / `compose_guided_resource_plan` / `resource_plan_authority`; exactly one stage calls `plan_evidence_from_canonical` and it is `_commit_planned_outcome`; the node delegates to every stage; plus a negative control proving the detector fails when a stage does compose a plan. Existing dual-runtime/shadow seam tests retained unchanged.
    **Verify (verbatim chain, one run):** `66 passed, 1 warning in 4.15s` (was 59 before the new controls) → `production_parity: total=120 base_105=105 exact=120 approved=0 critical=0`.
    **Drift-equivalence check beyond the gate:** the multiset of all side-effecting calls (`emit_*`, `save_handoff`, `save_clarification_handoff`, `plan_evidence_from_canonical`, `run_guided_detail_resolution`, `build_query_to_intent`, `evaluate_*`, `qualify_reference_query`, `build_canonical_planning_input`, `*_outcome`) is **identical** between the pre-refactor function body and the decomposed stages — no side effect added, dropped, or duplicated.
    **`/invariant-check`:** all seven groups PASS (no new `call_tool`/SPL-executability/EC/secret/state-key/flag changes; no test lines removed).
    **Commit:** `85a4c32`

- [x] **G0 — Correct architecture and operator documentation**
  - **Do:** Update `docs/architecture/architecture_review_2026-08-08.md`, `docs/architecture/mcp_tool_routing.md`, and `docs/architecture/chat_pipeline_state_v2_and_node_trace.md` with validated/corrected verdicts and the specialist amplification finding; correct the SPL ladder and dispatch-v2 default/current-posture summaries in `CLAUDE.md` without duplicating existing branch comments. Documentation must distinguish the fenced legacy Resource Planner multi-hop discovery/observer flow from live bounded pre-SPL MCP discovery under the dispatch-v2 SPL path; do not claim “MCP discovery never runs.” Rebuild both architecture HTML copies around D11–D13: show one uncluttered active-only `T1–T3` traversal and one active-only `T4→T0` traversal; route both through four explicit specialist audit cards, merge, and then a D1–D4 decision; remove the incorrect T0 bypass around specialists/merge. Use these primary labels (implementation names may appear as smaller secondary text or in the detailed section): Stage 1 “Receive the question and carry forward conversation context”; Stage 2 “Recognize what kind of SOC request this is”; Stage 3 “Decide whether to answer, ask for missing information, or stop”; Stage 4 “Run four independent plan-readiness checks”; Stage 5 “Combine the reports without changing the approved plan”; Stage 6 “Choose exactly one work path”; Stage 7 “Prepare and validate a safe search, bind its data source, then require the execution gate”; Stage 8 “Check evidence and safety, decide what may be claimed, require human review when needed, build the analyst card, and validate it.” Rename the specialist cards in plain language to **Routing Contract Auditor**, **Knowledge Coverage Auditor**, **MCP Readiness Auditor**, and **SPL Readiness Auditor**, with one-sentence inputs/outputs and “does not” boundaries; separately label downstream RAG as **Knowledge Retrieval Worker**. Replace the nine-node governance row whose captions overlap with a readable stacked or 3×3 checklist. Fix colour semantics so deterministic T0/reference work does not use the LLM-only iris colour. At 390 px, primary flow groups must reflow without horizontal scrolling, clipping, or sub-12px primary labels. Add `backend/app/tests/test_architecture_details_flow_contract.py` (**NEW**) to assert both HTML copies are identical, both requested traversals and D1–D4 mapping exist, specialist role names/boundaries exist, the obsolete D0 label and T0 bypass are absent, and accessible group names/descriptions summarize the actual decisions. Document `alert_summary` no-SPL policy, telemetry closed catalog, meaningful MCP/SPL report fields, and specialist no-I/O/no-authority boundaries. Do not change eval baselines. Build the frontend because `frontend/public` changes are production-served through `frontend/dist`.
  - **Verify:** `cd /var/www/ai-soc-assistant && cmp -s docs/architecture/details.html frontend/public/docs/architecture/details.html && PYTHONPATH=backend:. python3 -m pytest backend/app/tests/test_architecture_details_flow_contract.py -q && rg -n "Routing Contract Auditor|Knowledge Coverage Auditor|MCP Readiness Auditor|SPL Readiness Auditor|Knowledge Retrieval Worker|D4.*non-planned|planned_hop_count|candidate_tool_names|slot_binding_status|binding_candidate_tier|dispatch-v2|pre-SPL" docs/architecture/architecture_review_2026-08-08.md docs/architecture/mcp_tool_routing.md docs/architecture/chat_pipeline_state_v2_and_node_trace.md docs/architecture/details.html frontend/public/docs/architecture/details.html CLAUDE.md && cd frontend && npm run build`
  - **Depends on:** F0
  - **Note:** G0 legitimately changes the published doc mirrors. After the frontend build, re-run `python3 scripts/freeze_execution_baseline.py --capture --out /tmp/exec-baseline.json` so G1 checks against the intended post-doc state, and record in Evidence that the only changed group was `published_doc_mirrors`.
  - **Evidence:** **CLOSED 2026-08-10.**
    **Anchor correction — the diagram half was already done.** `docs/architecture/details.html` and its two mirrors already satisfied D11–D13 from the early user-authorized page work, and `backend/app/tests/test_architecture_details_flow_contract.py` already existed and passed (`7 passed`). G0's remaining work was therefore documentation only. No HTML was rewritten.
    **Corrected review claims** — appended a "Resolution" section to `docs/architecture/architecture_review_2026-08-08.md` recording the outcome of all seven findings (B0/B1 tier authority, C0 marker semantics ×2, E1 telemetry, D1/D2 specialists, E0 answer-mode, F0 decomposition), plus the **defect the review missed**: the `operator.add` specialist amplification (16,384 T0 / 8,192 T2-T4 against four lanes, exponential not linear, masked by `assert >= 4`), with the two transferable lessons (a `>=` is not a cardinality test; measure with a reachable database or the run takes a different graph path).
    **Discovery-flow correction (the important one).** `docs/architecture/mcp_tool_routing.md` now opens with a scope banner distinguishing the **fenced** legacy multi-hop loop (`canonical_forbids_legacy_evidence_planning`; `MAX_MCP_HOPS`, chronology, data-silence advisory, O5c recipes and `evidence_observer` do not run live) from **live bounded pre-SPL discovery** under `AI_SOC_PIPELINE_DISPATCH_V2_ENABLED`, which does reach the SPL plan compiler. It states explicitly that both "MCP discovery never runs" and "the discovery loop runs" are wrong.
    **`docs/architecture/chat_pipeline_state_v2_and_node_trace.md`** carries a staleness banner: `langgraph_orchestration_enabled` now defaults true, the RP graph is the spine, canonical planning is unconditional, the imperative path is rollback-only — while the field inventory and `node_trace` schema remain the valid C1/C9 reference.
    **`CLAUDE.md`** — SPL ladder line corrected to state the branch is **conditional, not linear** (mapped catalogue pattern with no rendered template takes the lab draft *before* the LLM), pointing at the existing call-site comment rather than duplicating it; dispatch-v2 row corrected from a bare "default false, flag-off byte-identical" to note that is a repo default and **the flag is enabled on the COE host**, where pre-SPL MCP discovery runs. Added four boundary bullets: specialist no-I/O/no-authority contract and the meaningful MCP/SPL report fields; `alert_summary` no-SPL fail-closed policy and ordered answer-mode precedence; the closed 8+20 telemetry catalog; and the `initial_tier` / `resolved_tier` / `binding_candidate_tier` / `observed_` vs `effective_match_path` vocabulary.
    **Verify (verbatim chain):** mirrors `cmp` identical → `7 passed in 0.33s` → `rg` matched all six files (`details.html` 18, mirror 18, `mcp_tool_routing.md` 3, `CLAUDE.md` 3, review 2, node-trace 2) → `npm run build` succeeded with `postbuild` chmod applied (`frontend/dist` `drwxr-xr-x`, dist mirror byte-identical to source).
    **Manifest:** `protected artifacts unchanged (13 checked)` — **the G0 re-capture note did not apply this time.** No protected artifact changed: `details.html` was already at its final content from `6ec8c9c`, and every file edited in this item is outside the protected set. No baseline was refreshed.
    **Live visual verification at both breakpoints** (Playwright against `https://cisco-vai.vnudge.com/docs/architecture/details.html`, page SHA256 `d33b274e68d0dd8f…` identical across all three mirrors). Measured rather than eyeballed:
    - 1440×1000 — `documentElement.scrollWidth - clientWidth = 0`; **zero** elements wider than the viewport; all five role names present (`Routing Contract Auditor`, `Knowledge Coverage Auditor`, `MCP Readiness Auditor`, `SPL Readiness Auditor`, `Knowledge Retrieval Worker`); obsolete `D0` label **absent**; 5 SVGs.
    - 390×844 — page-level horizontal overflow **0**. The only elements exceeding viewport width are `<table>`s, and every one sits in an `overflow-x: auto` parent, so the tables scroll inside their own container and the body does not. Smallest primary-label font is ≥12px: all 10 sub-12px nodes are `.arch-code` implementation identifiers (`specialist_skill`, `rag_early`, `composed_dispatch`, `spl_source_resolve`, `non_planned_finalize`, …) plus one 11px caption — exactly the "implementation names may appear as smaller secondary text" allowance in this item's Do.
    **Screenshot caveat (stated rather than glossed):** `g0-desktop-1440x1000.png` and `g0-mobile-390x844.png` were captured but written to the Playwright MCP host's own output directory, which is not this filesystem, so they are **not** attached in-repo. The acceptance above rests on the measured DOM assertions, which are reproducible and stricter than an image. Anyone wanting the images can re-run the two captures against the same URL.
    **Commit:** `a717d4c`

- [x] **G1 — Final invariant, regression, backend, parity, and plan re-audit gate**
  - **Do:** Re-walk every completed item against its Verify field, inspect the complete diff for baseline noise/secrets/authority expansion, run `/invariant-check`, then run targeted combined tests, canonical governance regression, full backend pytest, reference `--check`, and production parity. Record exact counts. Do not mark the plan done if any item lacks Evidence or if any baseline changed.
  - **Verify:** run from the repo root with the host-reachable database **exported for the whole chain** — `.env` carries the Docker-internal hostname `postgres`, which does not resolve on the host, and a per-command `DATABASE_URL=` prefix does not reach the governance script or pytest (corrected 2026-08-10; see Drift log).
    `cd /var/www/ai-soc-assistant && export DATABASE_URL=postgresql://ai_soc:ai_soc_dev_password@127.0.0.1:5434/ai_soc_assistant && .cursor/hooks/audit-plan-discipline.sh plans/2026-08-08_1824_architecture-review-corrective-actions.md && TELEMETRY_MODE=none PYTHONPATH=backend:. python3 scripts/audit_reference_probes.py --check && PYTHONPATH=backend:. python3 scripts/run_production_parity_eval.py --out-dir /tmp/architecture-corrective-final-parity --check && python3 scripts/freeze_execution_baseline.py --check --in /tmp/exec-baseline.json && ./scripts/run_stage3_governance_regression.sh && cd backend && PYTHONPATH=../backend:.. python3 -m pytest && cd ../frontend && npm run build`
  - **Depends on:** G0
  - **Evidence:** **CLOSED 2026-08-10.**
    **Plan re-audit:** `.cursor/hooks/audit-plan-discipline.sh` → `16 checked, 0 unchecked, 0 gap(s)`. Every item carries observed Evidence.
    **Reference probes (`--check`, non-mutating):** rc `0`, all ten rows compared, **10 PASS / 0 DRIFT** — P1-P4 `knowledge_recall`/`rag_only`, P5 `knowledge_recall`/`live_investigation`, P6+N1 `spl_generation`/`live_investigation`, N2+N4 `knowledge_recall`/`clarification`, N3 `knowledge_recall`/`live_investigation`. The four drifts A0 recorded as C0 input (P5, N1, N2, N4) are resolved; the frozen baseline was not refreshed.
    **Production parity (`--check`):** `total=120 base_105=105 exact=120 approved=0 critical=0`.
    **Protected manifest:** `protected artifacts unchanged (13 checked)` — re-checked again *after* the governance regression, so no gate rewrote what it verifies. No eval baseline, golden answer, governed registry, or doc mirror moved across the whole plan.
    **Canonical governance regression:** **PASS** (rc `0`) — backend pytest `4795 passed, 2 skipped, 6 xfailed in 491.53s`, harness **6/6**, OT analyst-ask probes 6/6, cov.q046 `8 passed, 1 skipped`, LangGraph dual-run parity `15 passed`, SOC clean-answer `17 passed`, plus SPL template audit, Cisco power-grid catalogue gate, manifest promotion audit, 105-question operation map, and dispatch matrix all green.
    **Full backend pytest (separate, detached):** `4795 passed, 2 skipped, 6 xfailed, 2 warnings in 518.30s`. Up from the `4703` recorded at P0 — **+92 tests, 0 failures**. The two pre-existing retention failures noted at P0 stayed fixed.
    **Frontend build:** succeeded; `postbuild` chmod applied, `frontend/dist` is `drwxr-xr-x`, and the `details.html` dist mirror is byte-identical to source.
    **`/invariant-check` on the cumulative diff `95db1fe..HEAD` (9 commits, 41 files):** all seven groups **PASS**. No new `call_tool`/`splunk_run_query` call site; no `supports_tool_calling` change; no authority expansion — the only `execution_eligible=True` added is inside a `pytest.raises(ValidationError)` proving the model *rejects* it; `backend/app/demo/` untouched; no secrets; no new env flag or setting; no test weakened or deleted. The three new state keys (`catalogue_binding_candidate`, `observed_catalogue_match_path`, `effective_catalogue_match_path`) are each declared in the `ChatPipelineState` TypedDict, so the LangGraph path cannot silently drop them.
    **Gate failure and its cause (recorded, not smoothed over):** the first governance run failed with rc `1` — every one of 120 clean-answer rows `exception`, `ARTIFACT_WRITE_REFUSED`, `REGRESSION FAILED`. Root cause was **this item's own Verify command**, not the code: `DATABASE_URL` was a per-command prefix on the probe call, so the governance script and pytest inherited `.env`'s Docker-internal `postgres` hostname, which does not resolve on the host; canonical persistence then failed closed exactly as designed. `127.0.0.1:5434` was open and all three compose services up throughout. Verify corrected to `export` the endpoint for the whole chain; re-run passed. One failure, one diagnosis, one fix — no third variation attempted.
    **Commit:** `e5c1937`

## Protected artifacts — must not change during execution

Captured by `scripts/freeze_execution_baseline.py` in **P0** and re-checked after every behaviour-changing item. A verification run must never rewrite the thing it is verifying.

| Group | Why it is frozen |
|---|---|
| Eval baselines (`reference_knowledge_baseline.md`, `regression_baseline.md`, `paraphrase_baseline.md`, probe JSONs) | These are the comparison points. `scripts/audit_reference_probes.py` currently rewrites its own baseline on every run (no argparse, unconditional `OUT.write_text()`), which is exactly what A0 fixes. |
| 105 golden answers (`question_105_golden.jsonl`) | The in-catalogue contract. Byte-identical output is the regression signal. |
| Governed registries (`use_cases/catalog.json`, `skills/catalog.json`, `spl/templates.json`) | 65 / 19 / template governance. A refactor has no reason to touch these; if one does, the refactor changed behaviour. |
| Published doc mirrors (`docs/`, `frontend/public/`, `frontend/dist/`) | Must stay byte-identical to each other — the guard asserts mutual equality, not just individual stability. G0 re-captures deliberately after documentation edits. |

Drift is a **stop condition**, not a warning. If a change is intentional, say so explicitly and re-capture; never refresh as a side effect.

## Required invariants during implementation

- Four `Send` branches remain visible in `resource_planner_graph_edges()` and the specialist registry continues to validate exactly four descriptors.
- Specialist reports are advisory or proposed-validated metadata only; final Resource Plan, SPL validator, and MCP execution gate remain authorities.
- MCP/SPL specialist builders are pure with respect to external systems: zero connector, LLM, renderer, validator, persistence, or execution calls.
- All report names/lists are bounded; only safe identifiers and reason codes are emitted.
- COE/manual config precedence remains unchanged; registry/session/RAG may fill blanks only.
- Candidate SPL never becomes evidence or execution input; `execution_eligible` remains false in the specialist layer.
- A candidate MCP tool name is not a selected tool. Deterministic selection and RBAC stay at the existing execution-gate stage after SPL validation.
- Non-planned canonical outcomes retain no edge to SPL generation or MCP execution.
- No new inline imports except an already-documented circular dependency site.
- No eval baseline refresh or accidental `frontend/dist` permission regression.

## Verification gaps

None at authoring time. Every checklist item has an exact test or gate. Tests marked **NEW** are created by the corresponding item before its Verify command is run.

## Drift log

| Date | Note |
|---|---|
| 2026-08-08 | Plan created from review of `docs/architecture/architecture_review_2026-08-08.md` against `master@1681f90`. |
| 2026-08-08 | User directive locked: MCP and SPL specialists remain critical permanent fan-out lanes and must be made meaningful; removal/collapse is prohibited. |
| 2026-08-08 | Plan-discipline audit passed with 14 checklist items, 14 Verify fields, and 0 gaps; plan is ready for execution. |
| 2026-08-08 | Removed the separate loop-runner at user direction. This canonical plan is the sole implementation source. |
| 2026-08-08 | Live Playwright audit confirmed the current diagrams are not an adequate target: the requested T4→T0 traversal is only a note, the SVG bypasses specialists/merge for T0, the two walkthrough SVGs differ mainly by dimming, governance captions overlap at 1440 px, and 390 px diagrams clip horizontally. D11–D13 and G0 now pin plain-language, role-distinct, responsive replacement diagrams. |
| 2026-08-08 | User-authorized page correction implemented the G0 visual contract early with an accessible responsive architecture SVG (separate desktop/mobile layouts) backed by semantic plain-language HTML. The page explicitly labels MCP/SPL as fixed markers today; it does not claim D1/D2 specialist backend work is complete. G0 remains unchecked until its documentation and full verification gates are satisfied. |
| 2026-08-08 | Anti-regression hardening added: new **P0** freezes a SHA256 manifest of 13 protected artifacts via `scripts/freeze_execution_baseline.py` (**NEW**, drift-tested in both directions) and records observed starting counts; `--check` appended to A1/B0/C0/D0/E0/E1 Verify commands; new "Protected artifacts" section; G0 re-captures after its intentional doc-mirror change; G1 checks the manifest before the governance regression. A0 now depends on P0. |
| 2026-08-08 | Audit vs live code: all six source-review correction premises verified correct against `master@1681f90`. Referenced scripts/tests exist except `app/tests/test_mcp_tool_selector.py`, which does not exist — D1 Verify corrected to `test_mcp_tool_planner.py` (`select_mcp_tool` is additionally covered by `test_mcp_execution_gate.py`, already listed). |
| 2026-08-08 | A1 premise re-measured: amplification mechanism confirmed (`specialist_reports: Annotated[list, operator.add]` at `resource_planner_graph.py:143` + every node returning full state via `_record`), and `test_resource_planner_graph_skeleton.py:118` masks it with `>=`. Observed 64 reports / 16x, not 8,192-16,384; magnitude claim corrected in the premise table. |
| 2026-08-08 | A0 premise confirmed: `scripts/audit_reference_probes.py` has no argparse and calls `OUT.write_text()` unconditionally, so it rewrites `docs/evals/reference_knowledge_baseline.md` on every run. A non-mutating `--check` is genuinely required before C0/G1 can use it as a gate. |
| 2026-08-08 | Existing dirty-worktree files belong to the user and are outside plan authoring scope: `.claude/settings.local.json`, `backend/app/chat/detail_tools/__init__.py`, both architecture HTML copies, and the untracked architecture review. Execution must preserve/reconcile them rather than overwrite unrelated edits. |
| 2026-08-08 | **Execution stop during P0:** the shared worktree advanced concurrently from `1681f90` through `fa8bc9a` and `3134db3` to `ae0a1f9`. The frozen manifest initially captured and checked clean, then all three protected `published_doc_mirrors` changed together from SHA256 prefix `4af5c9d1ecec` to `45e23fdc56f4`. No execution command in P0 intentionally edits these files, and the baseline was not recaptured. The full backend pytest gate also stalled twice at the same test-order boundary while the tree was changing, so its result is non-authoritative. Per the protected-drift/premise-change stop conditions, P0 remains unchecked and A0 was not started. |
| 2026-08-09 | Retention gate blocker resolved in scoped pre-plan commit `757237f`: retention tests now use a disposable migrated schema; `14/14` retention and `35/35` integration pass; production purge behavior is unchanged. A0 then exposed a new premise gap: its offline probe disables LLM/MCP but not canonical persistence, so the exact `--check` gate fails against the host-unresolvable Docker database name. Two attempts failed on this same gate; baseline stayed byte-identical. Execution stopped before A1. |
| 2026-08-09 | A0 remedy corrected by user decision: do **not** disable canonical persistence. Run the audit with the established host-reachable PostgreSQL URL (`127.0.0.1:5434`) so audit-critical persistence retains its production fail-closed behavior. A valid run produced six PASS rows (P1–P4, P6, N3) and four DRIFT rows (P5, N1, N2, N4), while the baseline remained byte-identical. A0 Verify now captures the after-hash even when `--check` returns drift and requires all ten comparison rows; the four drifts are explicit C0 input. |
| 2026-08-09 | Prior executor process checks were invalid: sandbox PID isolation plus atomic-save writes made `lsof`/Codex-task inventory look idle while another session was still writing. The user observed `details.html` move `dbad9916 → f4e43be6 → d0d4b345`, including a transient torn state where source and mirrors differed. The user waited 60 seconds for stability, re-validated the final content, aligned all three mirrors at `d0d4b345…`, and committed the documentation-only animation as `3b790c4`; scratch `arch-t35.png`, `.playwright-mcp/`, and `output/` were excluded. |
| 2026-08-09 | After `3b790c4`, the manifest was re-captured as an early G0-scoped documentation re-capture and checks clean for all 13 artifacts; only `published_doc_mirrors` changed and all three are byte-identical. No eval baseline, governed registry, golden answer, or runtime code was refreshed. |
| 2026-08-09 | A0 corrected Verify passed: wrapper `0`, live comparison `1` with all ten rows, CLI tests `11 passed in 1.28s`, reference baseline before/after SHA256 identical at `ce142eea…b9021`, and manifest 13/13 clean. The four real drifts (P5/N1/N2/N4) remain C0 inputs; no baseline was updated. The detached waiter remained alive only because its `pgrep -f` pattern matched its own command line. |
| 2026-08-09 | A1 pre-start concurrency blocker cleared by explicit user confirmation that the parallel documentation session ended. `master` was reconciled at `6ec8c9c`; a fresh no-ignore manifest capture/check reports 13/13 unchanged. |
| 2026-08-09 | P0 closed on observed results: production parity `120/0/0`; governance PASS (backend `4703 passed`, harness `6/6`, clean answers `120/120`, Cisco `50/50`, dispatch `5/5`); separate full backend `4703 passed, 2 skipped, 6 xfailed` in `574.69s`; manifest 13/13 clean. A sandboxed governance attempt deadlocked at 1% and was discarded as non-authoritative; the host marker-file run completed normally. |
| 2026-08-09 | **A1 premise contradiction / stop:** failing-first final-state probes measured 16,384 specialist reports on T0 rag-only and 8,192 on T2, T3, and T4-guided paths, not the locked 64/16x figure. All four dynamic delegate→specialist edges are also absent from the documented edge set. New test result `5 failed in 2.33s`; no runtime implementation was made, A1 remains unchecked, and B0/B1 were not started. |
| 2026-08-10 | **A1 premise revised — executor was right, the plan was wrong.** Both contradiction claims independently reproduced: final-state cardinality is 16,384 (T0 rag_only) / 8,192 (T2, T3, T4) against 4 unique lanes, and `("resource_planner_delegate", specialist)` is absent from `resource_planner_graph_edges()` for all four specialists. Root cause of the bad 64/16x premise: it was measured without a reachable database, so canonical persistence failed closed, planning never reached `planned`, and the run took the short `non_planned_finalize` route (4 doubling nodes = `4·2^4` = 64). Same probe with `DATABASE_URL=…@127.0.0.1:5434/…` yields 16,384. Growth is exponential (doubling per post-merge node), not the linear 16x recorded. Merge-time snapshots (`planner_iteration.reports`, `work_bundle.specialist_reports`) are already exactly 4, so the whole defect is post-merge and the reducer is the only runtime change needed. A1 unblocked: premise corrected, the preserved failing-first test stands unchanged, and the missing fan-out edges are now an explicit A1 deliverable rather than a test accommodation. |
| 2026-08-10 | **A1 resume stop — content premise contradicted.** After keyed reduction, the unchanged test reached its previously masked content assertions and failed 4/32 cases. A post-hoc Knowledge rebuild from final state is not the pre-fix fan-out payload: its fill-blank proposal has already been applied downstream, so recomputation correctly returns no proposal. Separately, the existing base-class report annotations on `PlannerIteration`/`WorkBundle` drop specialist subtype fields during serialization. No safe runtime change can make the post-hoc recomputation equal without weakening fill-blank or conflict semantics. One A1 Verify failed (`4 failed, 28 passed`); no second attempt, invariant check, or commit was made; B0/B1 remain unstarted. |
| 2026-08-10 | **A1 second contradiction adjudicated — split, not blocked.** Claim 1 (Knowledge content mismatch) confirmed as a defect in this plan's own anti-regression pin: it rebuilt expected reports by re-invoking specialists on the *final* state, after Knowledge's fill-blank proposal had already been applied downstream. Pin corrected to compare against the payload captured at fan-out. Claim 2 (subtype fields dropped by `PlannerIteration`/`WorkBundle`) confirmed real but **deferred to new item A2**: `list[SpecialistReport]` makes Pydantic narrow subclasses, the behaviour is identical before and after the reducer, and grep proves no production reader — `_knowledge_reference_domains` (`pipeline.py:9532`) sources `reference_domains` from step `args_template`, not the report object. A1's parity assertion is rescoped to the final-state channel it actually governs. Reducer implementation reviewed and sound; two residual risks routed to the wider gates: it raises on conflicting reports (a hard failure on a live `/chat` path, so identical-replay must hold beyond the four probes) and it returns key-sorted rather than arrival order. |
| 2026-08-10 | Resume directive after checkpoint `53b8534`: execute A2 through G1 only. C0 boundary matching applies to all five marker groups; D1 tool candidates are independent of the fenced legacy multi-hop loop and preserve real execution-hop counts; G0 also covers MCP routing/state-trace docs and dispatch-v2 posture. The separate post-G1 architecture-gap plan, shadow planner, topology self-certification, execution-order semantics, decision-record cleanup, LLM bridge/promotion, `MAX_MCP_HOPS`, and shadow flags remain deferred. After G1 only, run one live T4 trace to observe whether the unchanged shadow hop executes and is discarded. |
| 2026-08-10 | **C0 stop — valid frozen reference gate failed twice.** Boundary matcher/direct tests and the exact 45-test selector pass. First host-DB comparison retained four pre-C0 drifts; the scoped repair closed N1/N2/N4 and all P5 route-shape fields except the panel. Second comparison is 9 PASS / 1 DRIFT (`P5 has_mitre_panel: True -> False`). Per the plan stop condition, D0–G1 were not started, the baseline was not refreshed, and the uncommitted C0 work is preserved for adjudication. |
| 2026-08-10 | **C0 P5 panel adjudication — frozen contract retained.** C0 stopped after two valid host-DB audits: first **6 PASS / 4 DRIFT**, then **9 PASS / 1 DRIFT** with only `P5 has_mitre_panel: True -> False` remaining. User/COE adjudication confirmed that a MITRE-oriented investigation must retain its MITRE panel, while unsupported or incomplete mappings remain explicitly statused and fail closed. The correction therefore restores only candidate/`requires_validation` rows through the existing evidence policy; it does not manufacture supported claims, change execution authority, or refresh the frozen baseline. |
| 2026-08-10 | **G1 Verify was defective and is corrected.** The chain set `DATABASE_URL` as a prefix on the `audit_reference_probes.py` command only, so `run_stage3_governance_regression.sh` and the full backend pytest inherited `.env`'s Docker-internal `postgres` hostname. From the host that name does not resolve, canonical persistence fails closed, and the clean-answer eval reports all 120 rows as `exception` -> `ARTIFACT_WRITE_REFUSED` -> `REGRESSION FAILED`. Observed once (rc=1) before the cause was identified; `127.0.0.1:5434` was confirmed open and all three compose services up, so this is an invocation defect, not a code regression and not a database outage. Remedy is the one already sanctioned for A0 on 2026-08-09 - use the host-reachable endpoint, never disable persistence - now applied by `export`ing it for the whole chain. The gate script reads no `.env` itself, so any host-side runner must export it. |
